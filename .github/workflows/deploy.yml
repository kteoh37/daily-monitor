name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  schedule:
    # Twice daily, at 13:00 and 22:00 UTC. The upstream haver-data auto-pull
    # runs at ~07:00 server-local (~12:00 UTC) but on some days only pushes
    # later in the afternoon — the 22:00 UTC firing catches those.
    - cron: '0 13 * * *'
    - cron: '0 22 * * *'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout dashboard
        uses: actions/checkout@v4

      - name: Checkout haver-data (public)
        uses: actions/checkout@v4
        with:
          repository: jasonzhixinglu/haver-data
          path: haver-data
          ref: main

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - name: Install Python deps
        run: pip install -r scripts/requirements.txt

      - name: Build panel JSONs from parquet
        run: python scripts/build_data.py --haver-data haver-data

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - run: npm run build
        env:
          GITHUB_ACTIONS: true

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist/

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
